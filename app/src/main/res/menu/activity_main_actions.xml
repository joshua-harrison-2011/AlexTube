<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/action_collapse"
        android:icon="@drawable/ic_action_collapse"
        android:title="@string/collapse_label"
        app:showAsAction="always"
        android:visible="true"/>
    <item android:id="@+id/action_expand"
        android:icon="@drawable/ic_action_expand"
        android:title="@string/expand_label"
        app:showAsAction="always"
        android:visible="false"/>
</menu>